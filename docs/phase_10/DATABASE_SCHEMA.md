# Phase 10 Database Schema

Migration 10 creates:

- `coco_pilot_runs`
- `formal_report_runs`

The first table stores grounded narratives, citations, source manifests, redaction summaries, provider status, warnings, and limitations. The second stores report format, file path, SHA-256 checksum, content fingerprint, generator version, and source manifest.
