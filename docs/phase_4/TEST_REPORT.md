# Phase 4 Test Report

The release gate requires warnings to be treated as errors through `pytest.ini`.

Final development test result:

```text
185 passed in 42.26s
```

Coverage includes:

- all Phase 0–3 regression tests;
- migration 4 fresh install, idempotency, rollback guard, and re-upgrade;
- exact artifact feature order and deterministic feature hashes;
- named-variety resolution before model execution;
- bounded variety adjustment;
- mature-fruit component conversions;
- deterministic model output for identical inputs;
- persistence and retrieval;
- exact product/unit actual comparison;
- API and OpenAPI contracts;
- sanitized intercropping aggregates and privacy checks;
- source and model checksums;
- Python and JavaScript syntax verification;
- archive extraction and retesting before delivery.
