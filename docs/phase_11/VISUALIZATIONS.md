# Interactive Visualizations

Every Chart.js panel receives a common control bar with:

- zoom in and out;
- reset view;
- PNG export;
- CSV export;
- full-screen mode.

Existing pan, wheel zoom, pinch zoom, hover tooltips, series legends, and timeline markers remain active. Chart colors were revised to the logo palette while weather intensity palettes remain scientifically distinct.
