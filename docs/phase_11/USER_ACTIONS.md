# User Actions

1. Extract the release into `C:\COCOAID\P11`.
2. Run `setup.bat` and wait for `PHASE 11 VERIFICATION PASSED`.
3. Run `test.bat` and confirm the complete isolated suite passes.
4. Run `check_weather_provider.bat`.
5. Run `run.bat` and open `http://127.0.0.1:8000`.
6. Test pointer and keyboard coconut rotation, every navigation section, chart exports, the dedicated Weather GIS, audio controls, CoCO-PILOT, and DOCX/PDF report generation.
