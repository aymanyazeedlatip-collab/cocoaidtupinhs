# Coconut Hologram and Motion

The full-screen entry page and workspace landing page include an SVG coconut digital-twin visualization. It supports:

- slow automatic rotation;
- pointer and touch dragging;
- keyboard rotation using arrow keys;
- scanning-line animation;
- restrained orbit rings;
- reduced-motion handling.

The visualization uses no remote model dependency and therefore remains available offline.
